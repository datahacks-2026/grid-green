# Baseline runtime — `pip install -r requirements.txt` should always succeed.
# Python 3.14: pydantic>=2.13 pulls pydantic-core wheels (cp314); older pydantic
# forces a from-source Rust build of pydantic-core and often fails.
#
# Heavy / optional libs live in requirements-extras.txt.

fastapi>=0.118.0,<0.140.0
starlette>=0.46.0
uvicorn[standard]>=0.34.0,<0.45
pydantic==2.13.2
pydantic-settings==2.13.1
httpx>=0.28.1,<0.29
python-dotenv>=1.0.1,<2
pandas>=2.2.3
numpy>=2.0
scikit-learn>=1.5.2
slowapi==0.1.9
mcp>=1.10,<2
pytest>=8.3.4,<9
google-generativeai>=0.8.6,<0.9
