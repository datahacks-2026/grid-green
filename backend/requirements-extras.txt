# Optional / heavy dependencies. Install only on machines that need them.
#
#   pip install -r requirements-extras.txt
#
# All of these are auto-detected at runtime — the app gracefully degrades
# to lighter alternatives if any of these are missing.

# Real Prophet forecaster (else: seasonal-naive baseline).
prophet==1.1.6

# Real semantic embeddings for RAG (else: TF-IDF cosine).
# Use this on Brev for the NVIDIA sponsor evidence run.
sentence-transformers==3.3.1

# Snowflake landing for EIA + Cortex VECTOR storage (else: SQLite + TF-IDF).
snowflake-connector-python==3.12.4

# W&B for experiment tracking on the Brev workload.
wandb==0.18.7

# AWS — optional SageMaker Processing launcher (`scripts/sagemaker_processing.py`).
# Keep out of the baseline requirements so local dev / CI stays lightweight.
boto3>=1.35.0,<2
sagemaker==2.257.1
# Query UC / SQL warehouse from local machine (smoke test + future app reads).
databricks-sql-connector>=4.2.3<5

# Upload export CSV to DBFS (scripts.upload_eia_export_to_databricks).
databricks-sdk>=0.40,<1
